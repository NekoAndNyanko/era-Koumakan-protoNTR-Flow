;----------------------------------------------------------------------------------------------------
;Takeout口上函数名汇总
;----------------------------------------------------------------------------------------------------

书写某角色口上时，将函数名中的U替换为对应角色编号即可。

去访问者家：
	去的过程中被玩家看到：
	@NTR_ROOM_KU_GOTO_SEEN(对象)
	去的过程中没被玩家看到：
	@NTR_ROOM_KU_GOTO(对象)
	进入访问者家之后：
	@NTR_ROOM_KU_GOTO_AFTER(对象)

在访问者家中：
