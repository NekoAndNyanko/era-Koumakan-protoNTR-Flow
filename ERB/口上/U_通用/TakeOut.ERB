;----------------------------------------------------------------------------------------------------
;和访问者去访问者家的口上
;----------------------------------------------------------------------------------------------------

;和访问者去他家经过玩家身边
@NTR_ROOM_KU_GOTO_SEEN(对象)
#DIM 对象

;和访问者去他家没有经过玩家身边
@NTR_ROOM_KU_GOTO(对象)
#DIM 对象
IF NTR_CHK_VISIBLE(位置_訪問者宅)
	TRYCALLLIST
		FUNC NTR_KOJO_K{CFLAG:对象:口上パターン}_10_0(对象)
		FUNC NTR_KOJO_K_10_0(对象)
	ENDFUNC
ENDIF

;和访问者去他家进入他家之后
@NTR_ROOM_KU_GOTO_AFTER(对象)
#DIM 对象

;----------------------------------------------------------------------------------------------------
;
;----------------------------------------------------------------------------------------------------