;-------------------------------------------------------------------------------
;与女仆妖精对话(拡張版) - 能力確認 実行判定
; RETURN 0 コマンドの実行不可
; RETURN 1 コマンドの実行可能
@COM420_11_ENABLE
RESULTS:0 = %"[11]-能力確認"%
;常に実行可能
RETURN 1
RETURN 0

;-------------------------------------------------------------------------------
;与女仆妖精对话(拡張版) - 能力確認
; RETURN 0 コマンドの実行に失敗
; RETURN 1 コマンドの実行に成功
@COM420_11
#DIM 女仆编号
FOR 女仆编号,0,妖精メイド雇用上限
	IF 妖精メイド採用状態:女仆编号 == 妖精メイド採用状態_雇用済
		DRAWLINE
		CALL FairyMaids_PrintStatus(女仆编号)
	ELSEIF 女仆编号 == 妖精メイド雇用上限 - 1
		PRINTFORMW %"妖精女仆「"+CALLNAME:MASTER+"先去雇佣一个女仆再来吧…」"%
	ENDIF
NEXT
RETURN 1
