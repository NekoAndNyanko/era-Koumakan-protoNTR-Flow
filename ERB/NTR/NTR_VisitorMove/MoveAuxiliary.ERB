@SAFE_NEXT_POS(nowPos, targetPos)
#FUNCTION
#DIM nowPos
#DIM targetPos

SIF nowPos > 位置编号最大值 || nowPos <= 0
	RETURNF -1
SIF targetPos > 位置编号最大值 || targetPos <= 0
	RETURNF -1
IF nowPos == targetpos
	RETURNF nowPos
ELSEIF CAN_MOVE(nowPos, targetPos)
	RETURNF CAN_MOVE(nowPos, targetPos)
ELSE
	RETURNF 前置目的地计算(nowPos, targetPos)
ENDIF
;RETURNF 前置目的地计算(nowPos, targetPos)

