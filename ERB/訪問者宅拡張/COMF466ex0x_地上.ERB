;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_01_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"大门"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_01(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 1
IF FLAG:訪問者宅の施錠状態 & 訪問者宅_玄関解錠済
	PRINTFORML 差不多该回到红魔馆了吧…？
	PRINTFORML %"[0] 回去 [1] 不回去"%
	$COM466_01_INPUT_LOOP
	INPUT
	SELECTCASE RESULT:0
		CASE 0
			IF FLAG:访问者当前位置 == 位置_訪問者宅
				PRINTFORML %CALLNAME:奴隷+"对"+NTR_NAME(0)+"说『要回一次红魔馆了』，征得同意后，就回去了"%
				PRINTFORMW %NTR_NAME(0)+"虽然感到很遗憾，但因为知道"+CALLNAME:奴隷+"还会回来的，所以没有特别挽留"%
			ELSE
				PRINTFORML %CALLNAME:奴隷+"给"+NTR_NAME(0)+"留下了『回红魔馆了』的纸条后，就回去了"%
			ENDIF
			CFLAG:奴隷:当前位置 = 1
		CASE 1
			PRINTFORML %CALLNAME:奴隷+"希望再得到"+NTR_NAME(0)+"更多的疼爱"%
		CASEELSE
			GOTO COM466_01_INPUT_LOOP
	ENDSELECT
ELSE
	PRINTFORML %"门锁着。"%
ENDIF
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_02_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"厕所"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_02(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 2
PRINTFORML %"厕所打扫地很干净"%
PRINTFORMW %"最近，打扫这里也是"+CALLNAME:奴隷+"的工作"%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_03_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %" "%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_03(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 3
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_04_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"地板上的门(ハッチ)"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_04(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 4
PRINTFORML %"地板上有一扇提门。"%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_05_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"客厅"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_05(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 5
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_06_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %" "%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_06(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 6
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_07_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"浴室"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_07(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 7
PRINTFORML %"宽敞的浴室。"%
PRINTFORML %""%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_08_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"厨房"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_08(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 8
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_09_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"卧室"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_09(奴隷)
#DIM 奴隷
CFLAG:奴隷:当前位置詳細 = 9
PRINTFORML %NTR_NAME(0)+"和"+CALLNAME:奴隷+"一起使用过的床 "%
PRINTFORML %"似乎还能闻到上面残留的淫靡香气……"%
PRINTFORML %" "%
RETURN 1

