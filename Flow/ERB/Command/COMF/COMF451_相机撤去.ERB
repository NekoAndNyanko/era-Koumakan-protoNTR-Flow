;-----------------------------------------------------------
;	451,相机撤去
;-----------------------------------------------------------
@COM451
;設置場所を初期化
TFLAG:相机设置場所 = 0
;TFLAG111の中身を設置時間から録画期間に変更
TFLAG:相机设置時刻 = TIME_PROGRESS(TFLAG:相机设置時刻)

RETURN 1
