;-------------------------------------------------------------------------------
;(訪問者宅にて)訪問者と会话コマンド(拡張版) - 通常会话 実行判定
; @return 0 コマンドの実行不可, 1 コマンドの実行可能
@COM460_01_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"[ 1]-何か話をして欲しい"%
;作成中なので実行不可にしておく
RETURN 0

;-------------------------------------------------------------------------------
;会话コマンド(拡張版) - 通常会话
; @return 0:コマンドの実行に失敗, 1:コマンドの実行に成功
@COM460_01
RETURN 0
