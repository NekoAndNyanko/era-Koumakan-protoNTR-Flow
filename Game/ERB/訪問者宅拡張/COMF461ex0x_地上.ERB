;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_01_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"玄関扉"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_01(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 1
IF FLAG:訪問者宅の施錠状態 & 訪問者宅_玄関解錠済
	PRINTFORML %"大门没有锁上……看来可以逃走了"%
	PRINTFORML %"[0] 逃跑 [1] 沉溺在被调教的喜悦中"%
	$COM461_01_INPUT_LOOP
	INPUT
	SELECTCASE RESULT:0
		CASE 0
			PRINTFORML %CALLNAME:奴隷+"从门口飞奔而出，一边祈祷不要被"+NTR_NAME(0)+"发现，一边逃走了……"%
			CFLAG:奴隷:現在位置 = 1
		CASE 1
			PRINTFORML %"真的不逃跑吗……？"%
			PRINTFORML %CALLNAME:奴隷+"的脸上满是红晕…"%
		CASEELSE
			GOTO COM461_01_INPUT_LOOP
	ENDSELECT
ELSE
	PRINTFORML %"大门被紧锁着"%
ENDIF
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_02_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"厕所"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_02(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 2
PRINTFORML %"厕所正在被打扫"%
PRINTFORML %NTR_NAME(0)+"说过不会让『商品』使用"%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_03_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %" "%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_03(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 3
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_04_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"床扉(ハッチ)"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_04(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 4
PRINTFORML %"地板上有提门。"%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_05_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"客厅"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_05(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 5
PRINTFORML %NTR_NAME(0)+"似乎不在。"%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_06_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %" "%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_06(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 6
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_07_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"浴室"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_07(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 7
PRINTFORML %"非常大的浴室。"%
PRINTFORML %""%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_08_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"厨房"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_08(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 8
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_09_ENABLE(奴隷)
#DIM 奴隷
RESULTS:0 = %"卧室"%
IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
	RETURN 1
ELSE
	RETURN 0
ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_09(奴隷)
#DIM 奴隷
CFLAG:奴隷:現在位置詳細 = 9
PRINTFORML %" "%
PRINTFORML %" "%
RETURN 1

