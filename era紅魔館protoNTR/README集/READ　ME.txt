作者様、いつも更新お疲れ様です。
パッチ・・・というかMAPの増設とかを考えて見直したら気づいたので修正してみました。
もし仕様だったらごめんなさい。
指摘している所でミスがあったらサーセン。

そして避難所で文章を代行して下さっている方々、本当にありがとうございます。
	※追記
	内容にミスがありましたので修正しました。

MOVEMENT内の@CAN_MOVE(ARG, ARG:1)を修正しました。
以下の内容を修正
	・[守衛小屋]からそのまま[大浴場]へ行けないようにしました。
	・[庭]からそのまま[咲夜私室]へ行けないようにしました。
	・[庭]からそのまま[メイド妖精詰所]へ行けないようにしました。
	・[庭]からそのまま[あなた私室]へ行けないようにしました。
	・[庭]からそのまま[レミリア私室]へ行けないようにしました。
	・[2Fトイレ]からそのまま[踊り場]へ行けないようにしました。
	※追記
	[庭]から二階私室への移動を消す際に[納屋]への移動を消してしまいました。
	確認不足でした、申し訳ありません。


COMF400内の@FARMOVE(ARG:0)を修正しました。
以下の内容を追記
	・[咲夜の部屋]からは[二階廊下]のみに行けるようにしました。
	・[メイド妖精詰所]からは[二階廊下]のみに行けるようにしました。
	・[あなたの部屋]からは[二階廊下]のみに行けるようにしました。

以下の箇所を無効化
	;湖からは正門のみ
	;IF (LOCAL:0 == 19)
		;RETURN 1
	;ENDIF
	(湖を通じてチルノ・大妖精の家に通じている為、袋小路になってない。)
	※追記
	ここを消すと湖から二階の移動する際、[広間][踊り場]をループしてしまう事が判明したので以下の式に変更しました。
	;9からは1を目指す
	ELSEIF (LOCAL:0 == 9)
	           ↓
	;9 + 19からは1を目指す
	ELSEIF (LOCAL:0 == 9) || (LOCAL:0 == 19)
	確認不足でした、申し訳ありません。

以下の3箇所の式を修正
①	;1Fトイレからは広間のみ
	IF (LOCAL:0 == 22)
	           ↓
	;1Fトイレからは広間のみ
	IF (LOCAL:0 == 23)

②	;2Fトイレからは踊り場のみ
	IF (LOCAL:0 == 23)
	           ↓
	;2Fトイレからは踊り場のみ
	IF (LOCAL:0 == 24)
	
③	;踊り場に直接行けない場合、現在地が13〜15なら12を目指す
	IF (LOCAL:0 == 13) || (LOCAL:0 == 14) || (LOCAL:0 == 15)
	           ↓
	;踊り場に直接行けない場合、現在地が13〜15 + 24なら12を目指す
	IF (LOCAL:0 == 13) || (LOCAL:0 == 14) || (LOCAL:0 == 15) || (LOCAL:0 == 24)
	
修正方法が思いつかなくて投げ捨てたもの。
	・不具合？：[咲夜私室][メイド妖精詰所][あなた私室][レミリア私室]からそのまま庭へ移動出来る不具合。
	  原因：MOVEMENT内の@CAN_MOVE(ARG, ARG:1)における
	        各部屋のRETURNFの値を1or2にしても直通で移動出来る。
	        消すと移動は12→11→2→3→10を通る。しかしそうすると覗きが出来なくなる。
      対応策：あなたを含めてみんな窓から飛び降りていると脳内補完する。
	※追記
	本スレ>>892様
	仕様だったのですね。教えて頂きありがとうございます。
	

もしかしたらやる事
・[湖][チルノの家][大妖精の家]のMAP上に表示。
	※追記
	試作のMAPを作ってみました。
	COMF400(MAP追加版)を入れると[湖][チルノの家][大妖精の家]がMAP上に表示されます。
