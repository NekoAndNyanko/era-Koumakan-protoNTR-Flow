;-------------------------------------------------
;ニューゲーム
;-------------------------------------------------
@EVENTFIRST
TARGET = 0
ASSI = 0

PBAND = 12

CALL DEFAULT_OPTION

;モード選択
$MODE_SELECT
PRINTL ★★モードを選択してください★★
PRINTL [0]START
PRINTL 
PRINTL 　※特別なオプション（複数選択可）
IF GETBIT(FLAG:ゲームモード, 1)
	PRINT 　☆
ELSE
	PRINT 　　
ENDIF
PRINTL [1]ふたなりモード　　プレイヤーと訪問者以外全員ふたなり化

IF GETBIT(FLAG:ゲームモード, 2)
	PRINT 　☆
ELSE
	PRINT 　　
ENDIF
PRINTL [2]爆乳モード　　　　プレイヤーと訪問者以外の胸が爆乳になる

IF GETBIT(FLAG:ゲームモード, 3)
	PRINT 　☆
ELSE
	PRINT 　　
ENDIF
PRINTL [3]精力的な訪問者　　初日から訪問者が現れる

IF GETBIT(FLAG:ゲームモード, 4)
	PRINT 　☆
ELSE
	PRINT 　　
ENDIF
PRINTL [4]NTR設定全部乗せ 　NTRオプションを全てON（長期滞在期間は3日）

$INPUT_LOOP
INPUT

IF RESULT >= 1 && RESULT <= 4
	INVERTBIT FLAG:ゲームモード, RESULT
	GOTO MODE_SELECT
ELSEIF RESULT != 0
	GOTO INPUT_LOOP
ENDIF

PRINTL 誰かになりきる？
PRINTL [0]いいや
PRINTL [1]うん

$INPUT_LOOP_1
INPUT
IF RESULT == 0
	CALL CUSTOM_CHARAMAKE(MASTER)
	SIF HAS_PENIS(MASTER)
		MAXBASE:MASTER:5 = 1500
	ADDCHARA 1
	ADDCHARA 2
	ADDCHARA 3
	ADDCHARA 4
	ADDCHARA 5
	ADDCHARA 6
	ADDCHARA 7
	ADDCHARA 8
	ADDCHARA 9
	ADDCHARA 10
	ADDCHARA 11
	ADDCHARA 12
	ADDCHARA 13
ELSEIF RESULT == 1
	CALL REVERSEMODE_1
ELSE
	GOTO INPUT_LOOP_1
ENDIF

IF GETBIT(FLAG:ゲームモード, 1)
	FOR LOCAL, 1, CHARANUM
		TALENT:LOCAL:性別 = 3
	NEXT
ENDIF
IF GETBIT(FLAG:ゲームモード, 2)
	FOR LOCAL, 1, CHARANUM
		TALENT:LOCAL:バストサイズ = 2
	NEXT
ENDIF
IF GETBIT(FLAG:ゲームモード, 4)
	INVERTBIT FLAG:ＮＴＲパッチ設定, 1
	INVERTBIT FLAG:ＮＴＲパッチ設定, 3
	INVERTBIT FLAG:ＮＴＲパッチ設定, 4
	INVERTBIT FLAG:ＮＴＲパッチ設定, 5
	INVERTBIT FLAG:ＮＴＲパッチ設定, 6
	INVERTBIT FLAG:ＮＴＲパッチ設定, 7
	INVERTBIT FLAG:ＮＴＲパッチ設定, 8
	INVERTBIT FLAG:ＮＴＲパッチ設定, 24
	INVERTBIT FLAG:ＮＴＲパッチ設定, 25
	INVERTBIT FLAG:ＮＴＲパッチ設定, 26
	INVERTBIT FLAG:ＮＴＲパッチ設定, 31
	INVERTBIT FLAG:ＮＴＲパッチ設定, 32
	FOR LOCAL, 1, CHARANUM
		CALL NTR_SET_STAYOUT_MAXIMUM(LOCAL, 3)
	NEXT
ENDIF

SAVESTR:10 = /

;キャラカスタム選択
CALL CHARA_CUSTUM
SIF HAS_PENIS(MASTER)
	MAXBASE:MASTER:5 = 1500

;全員処女モード選択
CALL VIRGIN_CUSTOM

;◆ショートカットモード選択
CALL SHORT_CUT_MODE
;◆◆

FOR LOCAL,0,CHARANUM
	;開始位置
	CFLAG:LOCAL:現在位置 = CFLAG:LOCAL:311
	;服装
	CALL CLOTHES_SETTING(LOCAL,1)
NEXT


;起床時間
CFLAG:MASTER:312 = 420
CFLAG:MASTER:睡眠 = 1
DAY = 1
MONEY = 1000
BEGIN SHOP

@EVENTLOAD
CALL VERSION_UP

