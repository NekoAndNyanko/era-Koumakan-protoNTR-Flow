;-----------------------------------------------------------
;	451,カメラ撤去
;-----------------------------------------------------------
@COM451
	;設置場所を初期化
	TFLAG:カメラ設置場所 = 0
	;TFLAG111の中身を設置時間から録画期間に変更
	TFLAG:カメラ設置時刻 = TIME_PROGRESS(TFLAG:カメラ設置時刻)

RETURN 1
