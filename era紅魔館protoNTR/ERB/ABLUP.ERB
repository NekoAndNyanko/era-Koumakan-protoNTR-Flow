;;Ｃ感覚
;;@ABLUP0
;;CALL ABL_UP(0)
;
;;Ｖ感覚
;;@ABLUP1
;;CALL ABL_UP(1)
;
;;Ａ感覚
;;@ABLUP2
;;CALL ABL_UP(2)
;
;;Ａ感覚
;;@ABLUP3
;;CALL ABL_UP(3)
;
;;従順
;;@ABLUP10
;;CALL ABL_UP(10)
;
;;欲望
;;@ABLUP11
;;CALL ABL_UP(11)
;
;;技巧
;;@ABLUP12
;;CALL ABL_UP(12)
;
;;奉仕精神
;;@ABLUP13
;;CALL ABL_UP(13)
;
;;露出癖
;;@ABLUP14
;;CALL ABL_UP(14)
;
;;マゾッ気
;;@ABLUP15
;;CALL ABL_UP(15)
;
;;サドッ気
;;@ABLUP16
;;CALL ABL_UP(16)
;
;;レズッ気
;;@ABLUP17
;;CALL ABL_UP(17)
;
;;ホモッ気
;;@ABLUP18
;;CALL ABL_UP(18)
;
;;自慰中毒
;;@ABLUP30
;;CALL ABL_UP(30)
;
;;精液中毒
;;@ABLUP31
;;CALL ABL_UP(31)
;
;;レズ中毒
;;@ABLUP32
;;CALL ABL_UP(32)
;
;;ゲイ中毒
;;@ABLUP33
;;CALL ABL_UP(33)
;
;;汎用処理
;@ABL_UP(ARG)
;DRAWLINE
;;上限
;CALL MAXABL(ARG)
;SIF RESULT
;	RETURN 0
;
;VARSET LOCAL
;;個別処理
;;奉仕精神
;SIF ARG == 13
;	PRINTFORML 従順{ABL:ARG + 1}LV以上必要
;;マゾッ気
;SIF ARG == 15
;	PRINTFORML 欲望{ABL:ARG + 1}LV以上必要
;;サドッ気
;SIF ARG == 16
;	PRINTFORML 欲望{ABL:ARG + 1}LV以上必要
;;自慰中毒
;SIF ARG == 30
;	PRINTFORML Ｃ感覚{ABL:ARG + 1}LV以上、露出癖{ABL:ARG + 1}LV以上必要
;;精液中毒
;SIF ARG == 31
;	PRINTFORML 奉仕精神{ABL:ARG + 1}LV以上必要
;;レズ中毒
;SIF ARG == 32
;	PRINTFORML レズっ気{ABL:ARG + 1}LV以上必要
;;ゲイ中毒
;SIF ARG == 33
;	PRINTFORML ホモっ気{ABL:ARG + 1}LV以上必要
;
;IF EXP_DEMAND(ARG,0,53) == 1
;	PRINTFORML 異常経験あり、かつ
;ELSEIF EXP_DEMAND(ARG,0,53) > 1
;	PRINTFORML 異常経験{EXP_DEMAND(ARG,0,43)}以上、かつ
;ENDIF
;FOR LOCAL,0,4
;	IF JUEL_DEMAND(ARG,LOCAL) == -2
;		PRINTL 能力不足
;		BREAK
;	ENDIF
;	SIF JUEL_DEMAND(ARG,LOCAL) < 0
;		CONTINUE
;	PRINTFORM [{LOCAL}] -
;	;必要な宝珠を探す
;	FOR LOCAL:1,0,100
;		IF JUEL_DEMAND(ARG,LOCAL,LOCAL:1) > 0
;			SIF JUEL:(LOCAL:1) < JUEL_DEMAND(ARG,LOCAL,LOCAL:1)
;				LOCAL:3 ++
;			PRINTFORM  %PALAMNAME:(LOCAL:1)%の珠×{JUEL_DEMAND(ARG,LOCAL,LOCAL:1)}
;		ENDIF
;	NEXT
;	;必要な経験を探す
;	FOR LOCAL:1,0,100
;		IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) > 0
;			SIF  EXP:(LOCAL:1) < EXP_DEMAND(ARG,LOCAL,LOCAL:1)
;				LOCAL:4 ++
;			;異常経験は先に処理した
;			SIF LOCAL:1 == 43
;				CONTINUE
;			PRINTFORM  %EXPNAME:(LOCAL:1)%
;			IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) == 1
;				PRINT が必要
;			ELSE
;				PRINTFORM {EXP_DEMAND(ARG,LOCAL,LOCAL:1)}以上
;			ENDIF
;		ENDIF
;	NEXT
;	PRINT ・・・
;	SIF LOCAL:3
;		PRINT  宝珠不足
;	SIF LOCAL:4
;		PRINT  経験不足
;	IF !LOCAL:3 && !LOCAL:4 && !LOCAL:5
;		LOCAL:(10 + LOCAL) = 1
;		PRINT ＯＫ
;	ENDIF
;	LOCAL:3 = 0
;	LOCAL:4 = 0
;	LOCAL:5 = 0
;	PRINTL 
;NEXT
;
;PRINTL [999] - やめる
;
;INPUT
;IF RESULT == 999
;	RETURN 0
;ELSEIF RESULT < 0 || RESULT > LOCAL
;	RESTART
;ELSEIF !LOCAL:(10 + RESULT)
;	PRINTW 条件を満たしていません
;	RESTART
;ENDIF
;
;FOR LOCAL,0,40
;	JUEL:LOCAL -= JUEL_DEMAND(ARG,RESULT,LOCAL)
;NEXT
;ABL:ARG += 1
;PRINTFORMW %ABLNAME:ARG%のレベルが{ABL:ARG}になりました
;RETURN 0
;
;@ISABLUP(ARG)
;#FUNCTION
;VARSET LOCAL
;;上限設定
;IF ABL:ARG >= 99
;	RETURNF 0
;ELSEIF ABL:ARG >= 5 && !TALENT:恋慕
;	RETURNF 0
;ENDIF
;
;FOR LOCAL,0,4
;	SIF JUEL_DEMAND(ARG,LOCAL) == -2
;		RETURNF 0
;	SIF JUEL_DEMAND(ARG,LOCAL) < 0
;		CONTINUE
;	;必要な宝珠を探す
;	FOR LOCAL:1,0,100
;		IF JUEL_DEMAND(ARG,LOCAL,LOCAL:1) > 0
;			SIF JUEL:(LOCAL:1) < JUEL_DEMAND(ARG,LOCAL,LOCAL:1)
;				LOCAL:2 ++
;		ENDIF
;	NEXT
;	;必要な経験を探す
;	FOR LOCAL:1,0,100
;		IF EXP_DEMAND(ARG,LOCAL,LOCAL:1) > 0
;			SIF  EXP:(LOCAL:1) < EXP_DEMAND(ARG,LOCAL,LOCAL:1)
;				LOCAL:2 ++ 
;		ENDIF
;	NEXT
;	SIF !LOCAL:2
;		RETURNF 1
;	LOCAL:2 = 0
;NEXT
;
