;-------------------------------------------------
;訪問者を案内する
;-------------------------------------------------
@COM464
PRINTFORMW %NTR_NAME(0)%の姿を見つけ、%CALLNAME:MASTER%は%NTR_NAME(0)%に案内するからついてきて欲しいと声をかけた
; ここから他の住人が居る場合の処理
LOCAL:1 = 0
FOR LOCAL,1,CHARANUM
    IF CFLAG:MASTER:現在位置 == CFLAG:LOCAL:現在位置
        IF FLAG:訪問者のお気に入り == LOCAL
            PRINTFORMW しかし、今は%CALLNAME:LOCAL%と一緒に居るからと断られてしまった
            RETURN 0
        ENDIF
    ENDIF
NEXT
IF FLAG:訪問者のお気に入り == 0
    PRINTFORMW %NTR_NAME(0)%はもちろんと快諾してくれた
    FLAG:訪問者同行フラグ = DATETIME() + 180
ELSE
    PRINTFORMW %NTR_NAME(0)%は承諾し、%CALLNAME:MASTER%の傍に来た
    FLAG:訪問者同行フラグ = DATETIME() + 60
ENDIF
FLAG:訪問者との行為の終了時刻 = 0
FLAG:訪問者との行為 = NTR_行為_なし
