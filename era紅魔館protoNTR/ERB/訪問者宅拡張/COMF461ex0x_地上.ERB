;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_01_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"玄関扉"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_01(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 1
	IF FLAG:訪問者宅の施錠状態 & 訪問者宅_玄関解錠済
		PRINTFORML %"玄関の鍵はかかっていない……このまま逃げられそうだ"%
		PRINTFORML %"[0] 逃げる [1] 逃げない"%
		$COM461_01_INPUT_LOOP
		INPUT
		SELECTCASE RESULT:0
			CASE 0
				PRINTFORML %CALLNAME:奴隷+"は玄関を飛び出すと、"+NTR_NAME(0)+"に見つからないよう祈りながら逃げ帰った……"%
				CFLAG:奴隷:現在位置 = 1
			CASE 1
				PRINTFORML %"本当に逃げなくてもいいのだろうか……？"%
			CASEELSE
				GOTO COM461_01_INPUT_LOOP
		ENDSELECT
	ELSE
		PRINTFORML %"玄関の鍵は閉ざされている。"%
	ENDIF
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_02_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"トイレ"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_02(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 2
	PRINTFORML %"こちらのトイレは掃除されている。"%
	PRINTFORML %NTR_NAME(0)+"は『商品』には使わせない、と言っていた"%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_03_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %" "%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_03(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 3
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_04_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"床扉(ハッチ)"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_04(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 4
	PRINTFORML %"床には引き上げ式の扉がある。"%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_05_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"リビング"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_05(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 5
	PRINTFORML %NTR_NAME(0)+"は居ないようだ。"%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_06_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %" "%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_06(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 6
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_07_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"バスルーム"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_07(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 7
	PRINTFORML %"やたら広いバスルームだ。"%
	PRINTFORML %""%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_08_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"キッチン"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_08(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 8
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM461_09_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"ベッドルーム"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM461_09(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 9
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

