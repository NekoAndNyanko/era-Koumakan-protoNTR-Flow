;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_01_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"玄関扉"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_01(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 1
	IF FLAG:訪問者宅の施錠状態 & 訪問者宅_玄関解錠済
		PRINTFORML そろそろ紅魔館に戻ろうか…？
		PRINTFORML %"[0] 戻る [1] 戻らない"%
		$COM466_01_INPUT_LOOP
		INPUT
		SELECTCASE RESULT:0
		CASE 0
			IF FLAG:訪問者の現在位置 == 場所_訪問者宅
				PRINTFORML %CALLNAME:奴隷+"は"+NTR_NAME(0)+"に『一度紅魔館に帰ります』と挨拶して紅魔館に戻った"%
				PRINTFORMW %NTR_NAME(0)+"は少し残念そうだったが、"+CALLNAME:奴隷+"が戻ってくると分かっているからか特段引き留めはしなかった"%
			ELSE
				PRINTFORML %CALLNAME:奴隷+"は"+NTR_NAME(0)+"に『一度紅魔館に帰ります』と置手紙をして紅魔館に戻った"%
			ENDIF
			CFLAG:奴隷:現在位置 = 1
		CASE 1
			PRINTFORML %CALLNAME:奴隷+"はもう少しだけ"+NTR_NAME(0)+"に可愛がられることを望んだ"%
		CASEELSE
			GOTO COM466_01_INPUT_LOOP
		ENDSELECT
	ELSE
		PRINTFORML %"玄関の鍵は閉ざされている。"%
	ENDIF
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_02_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"トイレ"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_02(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 2
	PRINTFORML %"トイレはよく掃除されている。"%
	PRINTFORMW %"最近はここの掃除も"+CALLNAME:奴隷+"の仕事だ"%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_03_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %" "%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_03(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 3
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_04_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"床扉(ハッチ)"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_04(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 4
	PRINTFORML %"床には引き上げ式の扉がある。"%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_05_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"リビング"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_05(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 5
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_06_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %" "%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_06(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 6
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_07_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"バスルーム"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_07(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 7
	PRINTFORML %"やたら広いバスルームだ。"%
	SIF CFLAG:MASTER:WC_訪問者受け入れ具合 > 4
		PRINTFORML %"たまには"+NTR_NAME(0)+"とここに入るのもいいかもしれない"%
	PRINTFORML %""%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_08_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"キッチン"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_08(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 8
	PRINTFORML %" "%
	PRINTFORML %" "%
RETURN 1

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択肢
; @return 0 利用不可, 1 利用可能
@COM466_09_ENABLE(奴隷)
	#DIM 奴隷
	RESULTS:0 = %"ベッドルーム"%
	IF CFLAG:奴隷:NTR訪問者宅監禁状況 & 訪問者宅_床扉解錠済
		RETURN 1
	ELSE
		RETURN 0
	ENDIF

;-------------------------------------------------------------------------------
;訪問者宅マップ 選択結果
; @return 0 実行失敗, 1 実行成功
@COM466_09(奴隷)
	#DIM 奴隷
	CFLAG:奴隷:現在位置詳細 = 9
	PRINTFORML %NTR_NAME(0)+"と"+CALLNAME:奴隷+"のベッドだ "%
	PRINTFORML %"まだほんのりと淫靡な香りが残っている……気がする"%
	PRINTFORML %" "%
RETURN 1

