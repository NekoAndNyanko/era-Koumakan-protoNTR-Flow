@EVENT_COUNTER_MESSAGE(枝番)
#DIM 枝番
SIF TARGET <= 0
	RETURN 0
CALL DATUI_MESSAGE
RESULT = 0
IF 枝番
	TRYCALLFORM EVENT_COUNTE_MESSAGE{TCVAR:20}_{枝番}
ELSE
	TRYCALLFORM EVENT_COUNTE_MESSAGE{TCVAR:20}
ENDIF
SIF RESULT
	PRINTL

;色っぽい仕草
@EVENT_COUNTE_MESSAGE10
PRINTFORML %CALLNAME:TARGET%は色っぽい仕草で誘惑してきた…
RETURN 1

;見つめる
@EVENT_COUNTE_MESSAGE11
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%を潤んだ瞳でじっと見つめている…
RETURN 1

;体を摺り寄せる
@EVENT_COUNTE_MESSAGE12
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に体を摺り寄せてきた…
RETURN 1

;スカートめくり
@EVENT_COUNTE_MESSAGE13
PRINTFORML %CALLNAME:TARGET%は隙をついて%CALLNAME:PLAYER%のスカートを捲りあげた
IF !EQUIP:PLAYER:下半身下着２
	PRINTFORM スカートの中の何も着ていない下半身
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:PLAYER:下半身下着２)%%PANTSNAME(EQUIP:PLAYER:下半身下着２)%
ENDIF
PRINTL をバッチリ見られてしまった…
RETURN 1

;息を吹きかける
@EVENT_COUNTE_MESSAGE14
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の背後に忍び寄り耳元に熱い吐息を吹きかけた…
RETURN 1

;尻を撫でる
@EVENT_COUNTE_MESSAGE15
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のお尻を撫でてきた…
RETURN 1

;乳搾り手コキ
@EVENT_COUNTE_MESSAGE29_1
	IF EQUIP:PLAYER:下半身上着１ || EQUIP:PLAYER:下半身上着２
		IF EQUIP:PLAYER:下半身下着１ || EQUIP:PLAYER:下半身下着２
			PRINTFORMW %CALLNAME:TARGET+"は"+CALLNAME:PLAYER+"の手を壁に付かせると、下着ごと服を膝までずり下ろした。"%
		ELSE
			PRINTFORMW %CALLNAME:TARGET+"は"+CALLNAME:PLAYER+"の手を壁に付かせると、服を膝までずり下ろした。"%
		ENDIF
	ELSE
		IF EQUIP:PLAYER:下半身下着１ || EQUIP:PLAYER:下半身下着２
			PRINTFORMW %CALLNAME:TARGET+"は"+CALLNAME:PLAYER+"の手を壁に付かせると、下着を膝までずり下ろした。"%
		ELSE
			PRINTFORMW %CALLNAME:TARGET+"は"+CALLNAME:PLAYER+"の手を壁に付かせると、下着を穿いていない股間を撫で回した。"%
		ENDIF
	ENDIF
RETURN 1
@EVENT_COUNTE_MESSAGE29_2
	IF ABL:TARGET:指 > 2
		PRINTFORML %CALLNAME:TARGET+"は慣れた手付きで"+CALLNAME:PLAYER+"のペニスにコンドームを被せ、アヌスに指をねじ込んだ。"%
	ELSE
		PRINTFORML %CALLNAME:TARGET+"は"+CALLNAME:PLAYER+"のペニスにコンドームを被せ、アヌスに指をねじ込んだ。"%
	ENDIF
	IF CFLAG:PLAYER:ローターA挿入 != 0
		PRINTFORMW %"指先でローターを"+CALLNAME:PLAYER+"の前立腺に押し付けながら、牛の乳を搾るかのようにペニスを乱暴にしごき立て射精させる。"%
	ELSE
		PRINTFORMW %"指先で"+CALLNAME:PLAYER+"の前立腺を撫でながら、牛の乳を搾るかのようにペニスを乱暴にしごき立て射精させる。"%
	ENDIF
RETURN 1
@EVENT_COUNTE_MESSAGE29_3
	PRINTFORMW %CALLNAME:PLAYER+"が着衣の乱れを整えると、"+CALLNAME:TARGET+"はコンドームに溜まった精液を見せて意地悪く笑った……"%
RETURN 1

;キス
@EVENT_COUNTE_MESSAGE30
IF TALENT:TARGET:淫乱
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔に近づき、不意に唇を奪うとねっとりとした舌使いで口内を蹂躙した…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔に近づき、不意に唇を奪った…
ENDIF
RETURN 1

;後ろから抱きつく
@EVENT_COUNTE_MESSAGE31
PRINTFORML %CALLNAME:PLAYER%は背後から%CALLNAME:TARGET%に抱きつかれた
SIF TALENT:TARGET:バストサイズ > 0
	PRINTFORML %CALLNAME:PLAYER%の背中に%CALLNAME:TARGET%の柔らかな胸が押し当てられる…
RETURN 1

;前ろから抱きつく
@EVENT_COUNTE_MESSAGE32
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%を抱きしめ
IF TALENT:TARGET:体型 > TALENT:PLAYER:体型
	PRINTFORML その\@ TALENT:TARGET:バストサイズ > 0 ? 豊満な胸 # 胸 \@で%CALLNAME:PLAYER%の顔を包んだ…
ELSE
	PRINTFORML 息を荒らげながら体を摺り寄せてきた…
ENDIF
RETURN 1

;胸を揉ませる
@EVENT_COUNTE_MESSAGE33
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の手を取り、自分の胸に導いた
PRINTFORML %CALLNAME:TARGET%の心臓の鼓動が手から伝わってくる…
RETURN 1

;股間をまさぐる
@EVENT_COUNTE_MESSAGE34
IF TALENT:TARGET:淫乱
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の\@ HAS_PENIS(PLAYER) ? 股間に # 秘所に \@手を伸ばし巧みにまさぐり始めた…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の\@ HAS_PENIS(PLAYER) ? 股間に # 秘所に \@手を伸ばしまさぐり始めた…
ENDIF
RETURN 1

;スカートの中を見せる
@EVENT_COUNTE_MESSAGE35
PRINTFORML %CALLNAME:TARGET%は\@ TALENT:羞恥心 > 0 ? おずおずと # \@自分のスカートを捲り
PRINTFORM %CALLNAME:PLAYER%の目の前に
IF EQUIP:下半身下着１
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:下半身下着１)%%PANTSNAME(EQUIP:下半身下着１)%
ELSEIF !EQUIP:下半身下着２
	PRINTFORM スカートの中の何も着ていない下半身
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:下半身下着２)%%PANTSNAME(EQUIP:下半身下着２)%
ENDIF
PRINTFORML を晒した…
RETURN 1

;後ろから胸もみもみ
@EVENT_COUNTE_MESSAGE36
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の背後から胸を揉みしだいてきた…
RETURN 1

;手淫
@EVENT_COUNTE_MESSAGE50
IF HAS_PENIS(PLAYER)
	IF TCVAR:PLAYER:イきそう
		SIF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
			TEQUIP:PLAYER:3 = 1
		IF TEQUIP:シックスナイン
			PRINTFORM %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔にまたがったまま
		ELSE
			PRINTFORM %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔を見つめながら
		ENDIF
		IF TALENT:TARGET:サド
			PRINTFORML 激しく男性器をしごきあげじゅうぶんに焦らした後射精へと導いた…
		ELSE
			PRINTFORML 激しく男性器をしごきあげ射精へと導いた…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%は
		SIF TEQUIP:シックスナイン
			PRINTFORM %CALLNAME:MASTER%の顔にまたがったまま
		IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
			TEQUIP:PLAYER:3 = 1
			PRINTFORM 下着から男性器を取り出して
		ELSE
			PRINTFORM 露出させた男性器に
		ENDIF
		PRINTFORML 指を絡めてこすりだした…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%は
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM %CALLNAME:PLAYER%の下着をずらし、
	ENDIF
	PRINTFORML 顕になった%CALLNAME:PLAYER%の秘所を指でまさぐりだした・・・
ENDIF
RETURN 1

;口淫
@EVENT_COUNTE_MESSAGE51
IF HAS_PENIS(PLAYER)
	PRINTFORM %CALLNAME:TARGET%は
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM 下着から男性器を取り出して
	ELSE
		PRINTFORM 露出させた男性器を
	ENDIF
	IF TALENT:TARGET:淫乱
		PRINTFORML 口でくわえて巧みな舌使いでしゃぶりだした…
	ELSE
		PRINTFORML 口でくわえてしゃぶりだした…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%は
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM %CALLNAME:PLAYER%の下着をずらし、
	ENDIF
	PRINTFORML 顕になった%CALLNAME:PLAYER%の秘所に舌を這わせた…
ENDIF
RETURN 1

;素股
@EVENT_COUNTE_MESSAGE52
IF HAS_PENIS(PLAYER)
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に馬乗りになってペニスを秘所でこすり始めた…
ELSE
	PRINTFORML %CALLNAME:TARGET%は二人の性器を重ねてこすり始めた…
ENDIF
RETURN 1

;パイズリ
@EVENT_COUNTE_MESSAGE53
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の下着から男性器を取り出して
ELSEIF TEQUIP:PLAYER:0 & 2
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の露出させた男性器を
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の男性器を
ENDIF
PRINTFORML 胸で挟んでこすり始めた…
RETURN 1

;足コキ
@EVENT_COUNTE_MESSAGE54
IF TEQUIP:PLAYER:0 > 3 || (TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3)
	IF TALENT:TARGET:サド
		PRINTFORML %CALLNAME:TARGET%は微笑みながら%CALLNAME:PLAYER%の局部を踏みつけてこすり始めた…
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の局部を踏みつけてこすり始めた…
	ENDIF
ELSE
	IF TALENT:TARGET:サド
		PRINTFORML %CALLNAME:TARGET%は微笑みながら%CALLNAME:PLAYER%の男性器を踏みつけてこすり始めた…
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の男性器を踏みつけてこすり始めた…
	ENDIF
ENDIF
RETURN 1

;アナル愛撫
@EVENT_COUNTE_MESSAGE55
IF TALENT:TARGET:サド
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスに指をねじ込み愛撫した…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスを指で愛撫した…
ENDIF
RETURN 1

;アナル舐め
@EVENT_COUNTE_MESSAGE56
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスを丁寧に舐めはじめた…
RETURN 1

;指挿入
@EVENT_COUNTE_MESSAGE57
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の秘所にゆっくりと指を挿入した…
RETURN 1

;胸愛撫
@EVENT_COUNTE_MESSAGE58
IF TEQUIP:PLAYER:1 & 1 && !TEQUIP:PLAYER:4
	TEQUIP:PLAYER:4 = 1
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の胸をはだけ
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の
ENDIF
IF HAS_VAGINA(PLAYER)
	PRINTFORML 胸を揉みしだいた…
ELSE
	PRINTFORML 乳首を指でつまんでいじりだした…
ENDIF
RETURN 1

;乳首吸い
@EVENT_COUNTE_MESSAGE59
IF TEQUIP:PLAYER:1 & 1 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:4 = 1
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の胸をはだけ
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の
ENDIF
PRINTFORML 乳首に吸いついてきた…
RETURN 1

;ディープキス
@EVENT_COUNTE_MESSAGE60
IF TALENT:TARGET:淫乱
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔をつかんでくちづけし情熱的に舌を絡め唾液を流し込んできた…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔をつかんでくちづけし情熱的に舌を絡めてきた…
ENDIF
RETURN 1

;クンニ強制
@EVENT_COUNTE_MESSAGE70
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔にゆっくりとまたがり口元に秘所を押し付けた…
RETURN 1

;フェラチオ強制
@EVENT_COUNTE_MESSAGE71
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の口でペニスに奉仕させた…
RETURN 1

;アナル奉仕
@EVENT_COUNTE_MESSAGE72
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の顔にまたがって口元にアヌスを押し付けた…
RETURN 1

;ぱふぱふ
@EVENT_COUNTE_MESSAGE73
IF TALENT:TARGET:バストサイズ >= 0
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔を両側から%OPPAI_DESCRIPTION(TARGET)%で優しく包んだ…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%の顔を抱えて%OPPAI_DESCRIPTION(TARGET)%を押し付けた…
ENDIF
RETURN 1

;乳合わせ
@EVENT_COUNTE_MESSAGE74
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の体に胸を押し付けてきた…
RETURN 1

;タワシ洗い
@EVENT_COUNTE_MESSAGE75
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の腕にまたがって性器をこすりつけている…
RETURN 1

;挿入される
@EVENT_COUNTE_MESSAGE80
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の下着をずらし秘所にペニスをねじ込み、腰を動かし始めた…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の秘所にペニスをねじ込み、腰を動かし始めた…
ENDIF
IF TALENT:MASTER:処女 && TALENT:2 >= 1
	PRINTFORML 結合部からは%CALLNAME:PLAYER%の破瓜の血が流れ出している…
ELSE
	PRINTFORML 結合部からは%CALLNAME:TARGET%と%CALLNAME:PLAYER%の体液が溢れ出している…
ENDIF
RETURN 1

;後背位アナル
@EVENT_COUNTE_MESSAGE82
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%を四つん這いにすると
	PRINTFORM %CALLNAME:PLAYER%の
	SIF TEQUIP:PLAYER:0
		PRINT 下着をずらし、
	IF PALAM:PLAYER:潤滑 < PALAMLV:2
		PRINTFORML 湿り気の無いアナルに強引にペニスを押し込んだ…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%のアナルにペニスを挿入した…
	ENDIF
RETURN 1
;背面座位アナル
@EVENT_COUNTE_MESSAGE83
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%を背後から抱きかかえると
	PRINTFORM %CALLNAME:PLAYER%の
	SIF TEQUIP:PLAYER:0
		PRINT 下着をずらし、
	IF PALAM:PLAYER:潤滑 < PALAMLV:2
		PRINTFORML 湿り気の無いアナルに強引にペニスを押し込んだ…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%のアナルにペニスを挿入した…
	ENDIF
RETURN 1
;騎乗位アナル
@EVENT_COUNTE_MESSAGE81
	PRINTFORML %CALLNAME:TARGET%は横になると、%CALLNAME:PLAYER%に自分からペニスを咥え込むよう命令した。
	PRINTFORM %CALLNAME:PLAYER%は
	SIF TEQUIP:PLAYER:0
		PRINT 下着をずらし、
	IF PALAM:PLAYER:潤滑 < PALAMLV:2
		PRINTFORML 痛みを堪えながら湿り気の無いアナルにペニスを迎え入れた…
	ELSE
		PRINTFORML 自身の愛液で潤滑剤にアナルにペニスを迎え入れた…
	ENDIF
RETURN 1

;;騎乗位 文章は通常ウフフの騎乗位コマンドから拝借+α ずらし状態の扱いがわからなかったため保留
@EVENT_COUNTE_MESSAGE91
IF TEQUIP:TARGET:0  && !TEQUIP:TARGET:3
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の上にまたがって、下着をずらしゆっくりと腰を下ろしていった…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の上にまたがって、ゆっくりと腰を下ろしていった…
ENDIF
RETURN 1

;膣性交
@EVENT_COUNTE_MESSAGE500
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORML %CALLNAME:TARGET%は膣壁を操り%CALLNAME:PLAYER%のペニスをしごいてきた…
	;だいしゅきホールド
	CASE 2
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%に抱きつきながら両足で腰を抱え込んだ…
	;性交見せつけ
	CASE 3
		PRINTFORML %CALLNAME:TARGET%は上体をそらして%CALLNAME:PLAYER%に結合部を見せつけながらペニスをしごいている…
ENDSELECT
RETURN 1

;手淫
@EVENT_COUNTE_MESSAGE502
IF NOWEX:PLAYER:射精
	PRINTFORML %CALLNAME:TARGET%は溢れ出る先走りをペニス全体に広げると一気に速度を上げてペニスをしごきあげた…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスを握りゆっくりと上下させている…
ENDIF
RETURN 1

;フェラチオ
@EVENT_COUNTE_MESSAGE503
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスを咥え、唇で亀頭をしごいている…
	;ディープスロート
	CASE 3
		PRINTFORML %CALLNAME:TARGET%は限界まで勃起した%CALLNAME:MASTER%のペニスを根元まで飲み込み
		PRINTFORML 激しく頭を動かしている…
ENDSELECT
RETURN 1

;パイズリ
@EVENT_COUNTE_MESSAGE504
SELECTCASE TCVAR:21
	CASE 1
		IF TALENT:TARGET:バストサイズ > 0
			PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスを胸で挟んできた…
		ELSE
			PRINTFORML %CALLNAME:TARGET%は%CALLNAME:MASTER%のペニスに乳首をこすりつけている…
		ENDIF
	;縦パイズリ,乳ファック
	CASE 2
		PRINTFORML %CALLNAME:PLAYER%は%CALLNAME:TARGET%の豊満な胸の隙間にペニスを突き入れて腰を振った…
	;谷間を作る
	CASE 3
		IF TALENT:TARGET:バストサイズ < 0
			PRINTFORML %CALLNAME:TARGET%は平らな胸を懸命に寄せて何とかペニスを挟もうとしている…
		ELSE
			PRINTFORML %CALLNAME:TARGET%は胸を寄せて作った谷間でペニスを挟み上半身を激しく動かしている…
		ENDIF
ENDSELECT
RETURN 1

;素股
@EVENT_COUNTE_MESSAGE505
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORML %CALLNAME:TARGET%は太ももで%CALLNAME:MASTER%のペニスをはさみ腰を動かした…
	;手淫追加
	CASE 2
		PRINTFORML %CALLNAME:TARGET%は太ももで%CALLNAME:MASTER%のペニスをはさみ余った亀頭を指で責め立てた…
ENDSELECT
RETURN 1

;玉揉み
@EVENT_COUNTE_MESSAGE600
IF TCVAR:21 == 1
	IF SOURCE:加虐
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の陰嚢に手を伸ばし揉みほぐしてきた…
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の陰嚢に手を伸ばし優しく揉みほぐしてきた…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:TARGET%は陰嚢を咥えて口の中でコロコロと玉を転がしている
ENDIF
RETURN 1

;アナル愛撫
@EVENT_COUNTE_MESSAGE601
IF ABL:PLAYER:Ａ感覚 < 2
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスを指で刺激している…
ELSE
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアヌスに指を挿入し直腸までかき回した…
ENDIF
RETURN 1

@DATUI_MESSAGE
VARSET LOCAL
IF TCVAR:TARGET:22 == 1
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の下半身の衣服を下着ごと剥ぎとった
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 2
	IF TEQUIP:PLAYER:0 & 1
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のスカートの中に手を突っ込み
		PRINTFORML 中に履いている下着をずりおろした
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の下半身の衣服を下着ごと剥ぎとった
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 3
	IF TEQUIP:PLAYER:0 & 1
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のスカートの中に手を突っ込み
		PRINTFORML 中に履いている衣服をずりおろした
	ELSE
		PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の下半身の衣服を剥ぎとった
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT さらに
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:23 == 1
	PRINTFORML %CALLNAME:TARGET%は下半身の衣服を下着ごと脱ぎ捨てた
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 2
	IF TEQUIP:PLAYER:0 & 1
		PRINTFORML %CALLNAME:TARGET%はスカートの中に履いている下着を脱ぎ捨てた
	ELSE
		PRINTFORML %CALLNAME:TARGET%は下半身の衣服を下着ごと脱ぎ捨てた
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 3
	IF TEQUIP:PLAYER:0 & 1
		PRINTFORML %CALLNAME:TARGET%はスカートの中に履いている衣装を脱ぎ捨てた
	ELSE
		PRINTFORML %CALLNAME:TARGET%は下半身の衣服を脱ぎ捨てた
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT さらに
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:24 == 1
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の上半身の衣服を下着ごと剥ぎとった
	LOCAL = 1
ELSEIF TCVAR:TARGET:24 == 2
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の上半身の衣服を剥ぎとった
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT さらに
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:25 == 1
	PRINTFORML %CALLNAME:TARGET%は上半身の衣服を下着ごと脱ぎ捨てた
	LOCAL = 1
ELSEIF TCVAR:TARGET:25 == 2
	PRINTFORML %CALLNAME:TARGET%は上半身の衣服を脱ぎ捨てた
	LOCAL = 1
ENDIF
