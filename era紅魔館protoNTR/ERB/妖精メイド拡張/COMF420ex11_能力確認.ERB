;-------------------------------------------------------------------------------
;妖精メイドと話す(拡張版) - 能力確認 実行判定
; RETURN 0 コマンドの実行不可
; RETURN 1 コマンドの実行可能
@COM420_11_ENABLE
	RESULTS:0 = %"[11]-能力確認"%
	;常に実行可能
	RETURN 1
RETURN 0

;-------------------------------------------------------------------------------
;妖精メイドと話す(拡張版) - 能力確認
; RETURN 0 コマンドの実行に失敗
; RETURN 1 コマンドの実行に成功
@COM420_11
	#DIM メイド番号
	FOR メイド番号,0,妖精メイド雇用上限
		IF 妖精メイド採用状態:メイド番号 == 妖精メイド採用状態_雇用済
			DRAWLINE
			CALL FairyMaids_PrintStatus(メイド番号)
		ENDIF
	NEXT
RETURN 1
